# Re-review PR #62 after remediation

## Scope

Re-check `moisesja/didcomm-dotnet` PR #62 after the initial review at head
`0d0fe372762ee968501156d287313b0bf18f45df`, verify whether each requested
change was addressed, inspect any additional diff for regressions, and publish a
follow-up GitHub review disposition.

## Plan

- [x] Resolve the current PR head, metadata, discussion, review state, commits,
      changed files, and CI status; compare them with the previously reviewed
      revision and confirm the exact remediation delta.
- [x] Map the new changes to all four prior concerns: observation `with`-clone
      contract wording/tests, record equality/hash/string compatibility,
      snapshot precedence documentation and six-field coverage, and the
      optional `recipientAddressing` registration parameter/classification.
- [x] Inspect every added or modified line since `0d0fe37`, trace production
      behavior into callers and tests, and challenge new claims against the PRD,
      public XML docs, changelog, and actual C# record semantics.
- [x] Run focused validation for the affected suites and compatibility behavior;
      confirm GitHub CI independently and distinguish any pre-existing issue
      from a remediation regression.
- [x] Re-check every prospective finding against the current head and base,
      retaining only reproducible, merge-relevant concerns with exact paths and
      lines.
- [x] State the exact repository, PR, and current head before the GitHub write;
      publish a concise approval comment if all concerns are resolved, otherwise
      publish a specific follow-up requesting the remaining changes.
- [x] Re-read the published follow-up to verify it landed on PR #62 and accurately
      describes the current revision.
- [x] Record evidence, validation, and final disposition in this task file.

## Review / Results

Re-reviewed `moisesja/didcomm-dotnet` PR #62 at head
`b1889753d4462e4e9bf2ec8a892b470d64e5e6c3`, comparing both the complete PR
against base `dcabd53da33fb16e8d6a3e860e339a6276f6f5d1` and the remediation delta
against the previously reviewed head `0d0fe372762ee968501156d287313b0bf18f45df`.

Resolved from the first review:

- `recipientAddressing` is required at every verified registration site and
  issue #63 now accurately withdraws the misclassified item.
- Record value-equality semantics are acknowledged in upgrader notes and the
  top-level compatibility claim is narrowed to binary/API compatibility.
- Signed and authcrypt forged-clone tests collectively cover all six snapshot
  trust fields when verified content is unchanged.
- The public documents now scope observation/message pairing to observations as
  constructed by the library and acknowledge consumer `with`-clone behavior.

Remaining findings:

1. `FromUnpackResult` detects content divergence, nulls the snapshot, then falls
   back to the caller result's authentication flags and kids. It therefore pairs
   mutated/unverified content with `Authenticated = true`, `NonRepudiation = true`,
   and the original signer identity, contradicting the revised PRD/public contract.
2. The new XML docs incorrectly imply that caller-supplied bindings and
   `RecipientAddressing` are used without a covering snapshot; code always drops
   the bindings and forces addressing to `NotEvaluated`.
3. The `with`-clone limitation test substitutes another message with the same
   addressing outcome, so it does not prove stale/mismatched metadata; a nearby
   acceptance comment also still calls the value “unlaunderable.”

Validation:

- Remediation-focused Release tests: 75 passed, 0 failed.
- `git diff --check`: clean.
- Current GitHub CI: Ubuntu and Windows jobs passed.
- No new scalability, race, or cryptographic-path regression found.

Disposition: not approved. Published and verified a follow-up `COMMENTED` review
(the PR owner cannot formally request changes on their own PR):
https://github.com/moisesja/didcomm-dotnet/pull/62#pullrequestreview-4828502494

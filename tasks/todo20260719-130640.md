# PR #51 Review Plan

Target: `moisesja/didcomm-dotnet#51` — `feat(discover-features): initiator round-trip + inbound observer seam (#49, #50)`

Scope: review only. Do not modify the PR branch's source, tests, product documentation, or release metadata. The only external write authorized after plan approval is one GitHub PR review.

## Plan

- [x] Re-read the PR title, description, both commit messages, complete changed-file list, and complete diff against base SHA `a4938814025d2cd9b4d521d2fe13c3f756c06056` through current head `9bf97ad7667ef643e5f4c4bdf4eb8262d01fcf88`.
- [x] Map the intended behavior to the PRD and existing architecture, focusing on dispatcher outcome semantics, observer isolation, DI lifetimes/registration, Discover Features thread correlation, cancellation/timeout cleanup, and authenticated-sender binding.
- [x] Audit common review risks: missing or misleading tests, unclear commit history, excessive scope, public API compatibility, release/versioning correctness, concurrency/resource growth, exception handling, logging/privacy, and scalability.
- [x] Audit security-critical paths adversarially, including mutable-message isolation, observer privilege boundaries, spoofed or malformed disclosures, attacker-controlled message types, authentication metadata assumptions, denial-of-service surfaces, and observer-induced latency/failure.
- [x] Run focused tests and a Release build; inspect existing CI/check status if relevant. Do not treat self-reported PR results as proof.
- [x] Ask adversarial review agents to independently attempt to break the finished design, then reproduce or dismiss every finding from source evidence.
- [x] Classify findings by severity and provide exact file/line evidence, exploit or failure scenario, and required remediation. Avoid style-only comments unless they expose a concrete maintenance or correctness risk.
- [x] Draft exactly one GitHub review. Used a blocking `COMMENT` because GitHub does not permit the authenticated PR author to submit `REQUEST_CHANGES` on their own PR.
- [x] Restate the exact repository and PR target immediately before submitting the GitHub review, then verify that GitHub accepted it.
- [x] Add a review-results section below containing commands run, outcomes, findings, and the submitted review URL/status.

## Review Results

Completed against PR head `9bf97ad7667ef643e5f4c4bdf4eb8262d01fcf88`.

### Verification

- `dotnet build DidComm.sln -c Release --no-restore --nologo --tl:off -m:1` — passed, 0 warnings / 0 errors.
- `dotnet test DidComm.sln -c Release --no-build --no-restore --nologo --tl:off -m:1` — passed: 560 Core + 135 Interop = 695 tests.
- `gh pr checks 51 --repo moisesja/didcomm-dotnet` — Ubuntu and Windows build/test jobs passed.
- GitHub title and both commit messages are clear; combining #49/#50 is broad but cohesive.

### Blocking Findings

1. The advertised Discover Features round-trip cannot complete through either shipped transport: the
   responder reply is not delivered back into the initiator's dispatcher. The test manually injects it.
2. Replacing the public four-argument `ProtocolDispatcher` constructor with a five-argument signature
   is binary-breaking despite the minor-version/no-breaking-change claim.
3. Inline sequential observer callbacks can indefinitely block transport replies; cancellation can
   replace an already-computed outcome, and singleton-observer concurrency is undefined.
4. Identity-binding/correlation tests manufacture auth metadata rather than exercising real
   signed/authcrypt envelopes and the required tamper/forged-document cases.

Additional concerns: the query timeout begins only after send; DI observer wiring lacks coverage;
the PR verification count and cookbook claim are stale; `tasks/lessons.md` duplicates lesson ID L-033.

### Submitted Review

- State: `COMMENTED` with an explicit blocking / do-not-merge verdict (the authenticated user is the
  PR author, so GitHub does not allow a formal `REQUEST_CHANGES` self-review).
- URL: https://github.com/moisesja/didcomm-dotnet/pull/51
- Submitted at: 2026-07-19T17:22:07Z.
- The GitHub connector write was denied with HTTP 403; the authenticated `gh pr review` fallback
  succeeded, and `gh pr view` verified the review body and head SHA.

## Notes

- `CHANGELOG.md` updates are intentionally out of scope: this task audits an existing PR and must not mutate its product changes.
- Existing unrelated working-tree changes (`.claude/settings.json` and `tasks/todo20260617-163016.md`) will be preserved.
